
/* 1 Crea una función que calcule el índice de masa corporal (IMC) de una persona a partir de su 
altura en metros y peso en kilogramos, que se recibirán como parámetros.*/

// function indiceDeMasaCorporal(peso, altura) {
//    indice = peso / (altura * altura);
//    console.log(indice)
//    return;
// }

// indiceDeMasaCorporal(100, 1.80);


/* 2 Crea una función que calcule el valor del factorial de un número pasado como parámetro.*/



// function valorFactorial(numero) {

//    let incremento = 1;
//    let factorial = 1;
//    while (numero >= incremento) {

//       factorial *= incremento;
//       incremento++
//    }
//    console.log(factorial)
// }
// valorFactorial(6);


/* 3 Crea una función que convierta un valor en dólares, pasado como parámetro, y devuelva 
el valor equivalente en reales(moneda brasileña,si deseas puedes hacerlo con el valor del 
dólar en tu país). Para esto, considera la cotización del dólar igual a R$4,80. */


// function conversorDeDolarAPesos(dolar){
//    let pesos = 821.45;
//    let cotizacion;
//    cotizacion = pesos * dolar;
//    console.log(`el cambio de ${dolar} a ${pesos} es = ${cotizacion}`)
// }
   

// conversorDeDolarAPesos(10);




/* 4 Crea una función que muestre en pantalla el área y el perímetro de una sala rectangular, 
utilizando la altura y la anchura que se proporcionarán como parámetros.*/

// function areaYPerimetroDeRectangulo(alto, ancho){
//       perimetro = (alto * 2 ) + (ancho * 2);
//       area = ancho * alto;
//       console.log(`el perimetro de un rectangulo es ${perimetro} y su area es ${area}`);
// }

// areaYPerimetroDeRectangulo(10, 20);

/* 5 Crea una función que muestre en pantalla el área y el perímetro de una sala circular, 
utilizando su radio que se proporcionará como parámetro. Considera Pi = 3,14.*/

// function areaYPerimetroDeCirculo(radio){
//    let pi = 3.14;
//    perimetro = (pi * 2) * radio;
//    area = Math.pow(radio, 2) * pi; 
//    console.log(`el perimetro del circulo es ${perimetro} y su area ${area}`);
// }

// areaYPerimetroDeCirculo(10);

/* 6 Crea una función que muestre en pantalla la tabla de multiplicar de un número dado como 
parámetro. */

// function tablaDeMultiplicar(numero){
//    let incremento = 1;
//    let producto;
//    while (incremento <= 10){
//       producto = numero * incremento;
//       console.log(`${numero} por ${incremento} es igual a: ${producto}`)
//       incremento++
//    }
// }

// tablaDeMultiplicar(9);


